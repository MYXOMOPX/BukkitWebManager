import controller from "./app.map.ctrl.js"
import template from "./app.map.tpl.html"

export default {
    name:"app.map",
    controller:controller,
    template:template,
    controllerAs:"ctrl",
    url:"map"
}